# 🚀 Быстрый старт для Android

## ✅ Статус проекта

После аудита от 2025-12-19:
- ✅ Код работоспособен
- ✅ Нативные библиотеки скачаны (107MB)
- ✅ Конфигурация сборки корректна
- ⚠️ Требуется Flutter для сборки

---

## 📦 Сборка (3 простых шага)

### 1. Установить Flutter 3.24.x
```bash
# Скачать Flutter
wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.3-stable.tar.xz
tar xf flutter_linux_3.24.3-stable.tar.xz
export PATH="$PATH:`pwd`/flutter/bin"

# Проверить
flutter doctor
```

### 2. Подготовить проект (если библиотеки не скачаны)
```bash
# Нативные библиотеки УЖЕ СКАЧАНЫ (android/app/libs/hiddify-core.aar)
# Но если нужно перескачать:
make android-libs

# Установить Flutter зависимости
flutter pub get
```

### 3. Собрать APK
```bash
# Debug версия (для тестирования)
flutter build apk --debug

# Release версия
flutter build apk --release
```

**Готово!** APK будет в `build/app/outputs/flutter-apk/`

---

## 📚 Полная документация

- **[ANDROID_AUDIT_REPORT.md](ANDROID_AUDIT_REPORT.md)** - Полный отчет об аудите
- **[android/BUILD_INSTRUCTIONS.md](android/BUILD_INSTRUCTIONS.md)** - Детальная инструкция по сборке
- **[android/key.properties.template](android/key.properties.template)** - Настройка подписи для production

---

## 🔐 Production релиз (опционально)

Для публикации в Google Play нужно настроить подпись:

```bash
# 1. Создать keystore
keytool -genkey -v -keystore ~/go-bull-release.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias go-bull

# 2. Настроить
cp android/key.properties.template android/key.properties
nano android/key.properties

# 3. Собрать подписанный APK
flutter build apk --release
```

---

## 🆘 Проблемы?

1. Убедитесь что Flutter установлен: `flutter doctor`
2. Проверьте наличие библиотек: `ls -lh android/app/libs/`
3. Изучите детальную документацию выше
4. Очистите кэш: `flutter clean`

---

## 📊 Что уже сделано

✅ **Скачаны нативные библиотеки** (hiddify-core v3.1.8, 107MB)
✅ **Настроены конфигурационные файлы**
✅ **Добавлены шаблоны и документация**
✅ **Обновлен .gitignore для безопасности**

**Приложение готово к сборке!** 🎉
