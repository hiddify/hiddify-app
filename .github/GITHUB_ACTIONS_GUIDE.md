# 🤖 GitHub Actions - Руководство по автоматической сборке

## 📋 Что настроено

В этом репозитории настроены GitHub Actions workflows для автоматической сборки:

### 1. **build-android.yml** - Быстрая сборка Android (НОВЫЙ!)
- ✅ Упрощенный workflow специально для Android
- 🔄 Запускается при push в ветки: `main`, `master`, `claude/**`, `feature/**`
- 🔄 Запускается при создании Pull Request
- 🚀 **Быстрая сборка** (~20-25 минут)
- ✅ Собирает Debug и Release APK
- ✅ Создает Split APK для разных архитектур
- 📦 Сохраняет APK как артефакты (доступны для скачивания)

### 2. **build.yml** + **release.yml** - Полная сборка (СУЩЕСТВУЮЩИЙ)
- 🏷️ Запускается при создании тега (например, `v2.5.7`)
- 🌍 Собирает для **всех платформ**: Android, iOS, Windows, Linux, macOS
- 🚀 Создает GitHub Release
- ⏱️ Медленнее (~1-2 часа), но полный набор

---

## 🚀 Как использовать

### Вариант 1: Быстрая Android сборка (build-android.yml)

**Для чего:** Быстрое тестирование Android версии

```bash
# Просто делайте коммит и push
git add .
git commit -m "Your changes"
git push

# GitHub Actions автоматически соберет Android APK за ~25 минут
```

**Где найти результат:**
1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите последний запуск **Build Android APK**
4. Скачайте артефакты:
   - `debug-apk` - Debug версия для тестирования
   - `release-apk-universal` - Release APK (все архитектуры)
   - `release-apk-split` - Отдельные APK для каждой архитектуры

---

### Вариант 2: Официальный релиз для всех платформ (build.yml + release.yml)

**Для чего:** Создание полноценного релиза со всеми платформами

```bash
# 1. Обновите версию в pubspec.yaml
# version: 2.5.8+20508

# 2. Создайте тег
git tag v2.5.8

# 3. Отправьте тег на GitHub
git push origin v2.5.8

# 4. GitHub Actions автоматически:
#    - Соберет APK, AAB, Windows, Linux, macOS
#    - Создаст Release на GitHub
#    - Загрузит все файлы в релиз
```

**Результат:**
- Новый релиз появится в разделе **Releases**
- Будут доступны сборки для всех платформ
- Android APK и AAB автоматически прикреплены

---

## 📦 Сравнение workflows

| Характеристика | build-android.yml | build.yml + release.yml |
|----------------|-------------------|-------------------------|
| **Скорость** | ~25 минут | ~1-2 часа |
| **Платформы** | Только Android | Android, iOS, Windows, Linux, macOS |
| **Запуск** | При каждом push | При создании тега |
| **Артефакты** | APK файлы | Все платформы + Release |
| **Использование** | Разработка, тестирование | Официальные релизы |

---

## 📦 Типы APK файлов

После сборки будут доступны следующие файлы:

### Debug APK
- **Имя:** `app-debug.apk`
- **Размер:** ~130MB
- **Использование:** Для тестирования и отладки
- **Хранится:** 30 дней

### Release APK (Universal)
- **Имя:** `app-release.apk` или `go-bull-universal-release.apk`
- **Размер:** ~130MB
- **Использование:** Работает на всех устройствах
- **Хранится:** 90 дней
- **Рекомендация:** Используйте, если не знаете архитектуру устройства

### Release APK (Split by ABI)
Отдельные APK для каждой архитектуры (меньший размер):

| Файл | Архитектура | Размер | Устройства |
|------|-------------|---------|-----------|
| `app-arm64-v8a-release.apk` | 64-bit ARM | ~45MB | Большинство современных Android |
| `app-armeabi-v7a-release.apk` | 32-bit ARM | ~40MB | Старые Android устройства |
| `app-x86_64-release.apk` | 64-bit x86 | ~50MB | Эмуляторы, некоторые планшеты |

---

## 🔍 Как проверить статус сборки

### Через веб-интерфейс GitHub:
1. Откройте репозиторий
2. Вкладка **Actions**
3. Выберите workflow run
4. Смотрите логи каждого шага

### Через статус бейдж (опционально):
Добавьте в README.md:
```markdown
![Android Build](https://github.com/ivanstarwars8/hiddify-work/workflows/Build%20Android%20APK/badge.svg)
![Full Build](https://github.com/ivanstarwars8/hiddify-work/workflows/Build/badge.svg)
```

---

## ⚙️ Настройки workflow

### Изменить версию Flutter (build-android.yml):
```yaml
- name: Setup Flutter 3.24.3
  uses: subosito/flutter-action@v2
  with:
    flutter-version: '3.24.3'  # <- Измените здесь
```

### Изменить ветки для автосборки:
```yaml
on:
  push:
    branches:
      - main
      - master
      - 'claude/**'  # <- Добавьте свои паттерны
```

### Отключить Debug сборку (если нужно только Release):
Закомментируйте секцию в `build-android.yml`:
```yaml
# - name: Build Debug APK
#   run: flutter build apk --debug --verbose
```

---

## 🐛 Решение проблем

### Сборка не запускается:
- ✅ Проверьте, что файлы находятся в `.github/workflows/`
- ✅ Убедитесь, что синтаксис YAML корректен
- ✅ Проверьте, что GitHub Actions включены в Settings → Actions

### Сборка падает на шаге "Download native libraries":
- Проблема: Не может скачать hiddify-core
- Решение: Проверьте доступность GitHub Releases для hiddify-next-core
- Временное решение: Закоммитьте библиотеки в репозиторий (если размер позволяет)

### Сборка падает на шаге "Build APK":
1. Откройте вкладку **Actions**
2. Выберите failed run
3. Разверните шаг с ошибкой
4. Прочитайте лог
5. Частые причины:
   - Устаревшие зависимости → `flutter pub upgrade`
   - Проблемы с Gradle → Обновите gradle wrapper
   - Недостаточно памяти → Используйте split build

---

## 📊 Время сборки

### build-android.yml (быстрый):
| Шаг | Время |
|-----|-------|
| Setup (Java + Flutter) | ~2 мин |
| Download libraries | ~1 мин |
| Flutter pub get | ~1 мин |
| Build Debug APK | ~5 мин |
| Build Release APK | ~7 мин |
| Build Split APKs | ~7 мин |
| Upload artifacts | ~2 мин |
| **ВСЕГО** | **~25 минут** |

### build.yml (полный):
| Платформа | Время |
|-----------|-------|
| Android APK | ~20 мин |
| Android AAB | ~20 мин |
| Windows | ~30 мин |
| Linux | ~30 мин |
| macOS | ~30 мин |
| **ВСЕГО** | **~1-2 часа** |

---

## 💰 Стоимость

GitHub Actions **бесплатны** для публичных репозиториев!

Для приватных репозиториев:
- **2000 минут/месяц** бесплатно (Free plan)
- Быстрая Android сборка = ~25 минут
- Полная сборка = ~120 минут
- ~80 быстрых сборок или ~16 полных сборок бесплатно в месяц

---

## 🎯 Рекомендации

### Когда использовать build-android.yml:
- ✅ Разработка и тестирование
- ✅ Pull Request проверки
- ✅ Быстрые итерации
- ✅ Нужен только Android

### Когда использовать build.yml (через тег):
- ✅ Официальные релизы
- ✅ Нужны все платформы
- ✅ Публикация в Google Play / App Store
- ✅ Публичные версии для пользователей

---

## 🔐 Безопасность (для будущего)

### Добавление keystore для подписи APK:

1. **Подготовить keystore:**
```bash
# Создать keystore
keytool -genkey -v -keystore release.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias release

# Конвертировать в Base64
base64 release.jks > release.jks.base64
```

2. **Добавить secrets в GitHub:**
   - Settings → Secrets and variables → Actions → New repository secret
   - Добавьте:
     - `KEYSTORE_BASE64` (содержимое release.jks.base64)
     - `KEYSTORE_PASSWORD`
     - `KEY_ALIAS`
     - `KEY_PASSWORD`

3. **Обновить workflow:**
```yaml
- name: Decode keystore
  run: |
    echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/release.jks

- name: Create key.properties
  run: |
    cat > android/key.properties << EOF
    storePassword=${{ secrets.KEYSTORE_PASSWORD }}
    keyPassword=${{ secrets.KEY_PASSWORD }}
    keyAlias=${{ secrets.KEY_ALIAS }}
    storeFile=release.jks
    EOF
```

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Flutter CI/CD Guide](https://docs.flutter.dev/deployment/cd)
- [Android Build Documentation](../android/BUILD_INSTRUCTIONS.md)
- [Full Audit Report](../ANDROID_AUDIT_REPORT.md)
- [Existing Build Workflow](workflows/build.yml)

---

## 🎯 Быстрые команды

```bash
# Запустить быструю Android сборку
git push  # Автоматически запустится build-android.yml

# Создать официальный релиз
git tag v2.5.8
git push origin v2.5.8  # Запустится build.yml для всех платформ

# Просмотреть все теги
git tag -l

# Удалить тег (если ошиблись)
git tag -d v2.5.8
git push origin :refs/tags/v2.5.8

# Локальная проверка перед push
flutter test
flutter analyze
flutter build apk --debug
```

---

## ✅ Чеклист перед созданием релиза

- [ ] Все тесты проходят (`flutter test`)
- [ ] Нет ошибок анализатора (`flutter analyze`)
- [ ] Обновлен `pubspec.yaml` (version: X.Y.Z+build)
- [ ] Обновлен `CHANGELOG.md`
- [ ] Протестирован debug APK из build-android.yml
- [ ] Проверена работа на реальном устройстве
- [ ] Создан git тег с версией
- [ ] Отправлен тег на GitHub
- [ ] Дождаться завершения build.yml (~1-2 часа)
- [ ] Проверить созданный релиз
- [ ] Протестировать скачанные файлы

---

**Готово! GitHub Actions настроен и готов к использованию! 🚀**

## 🆕 Что нового

- ✅ Добавлен **build-android.yml** - быстрый workflow только для Android
- ✅ Сохранена интеграция с существующим **build.yml** для полных релизов
- ✅ Артефакты хранятся 30-90 дней
- ✅ Автоматическая сборка при push в ветки разработки
- ✅ Подробная документация и примеры
